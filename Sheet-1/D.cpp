#include<bits/stdc++.h>
using namespace std;

int main()
{
    long long A, B, C, D;
    //Take Input from Keyboard for (A,B,C,D)
    cin>> A >> B >> C >> D ;
    //Print the result of the following equation (A*B)-(C*D)
    cout<<"Difference = "<<(A*B)-(C*D);
    return 0;
}
